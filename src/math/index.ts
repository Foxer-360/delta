/**
 * Just for testing purposes. Simple sum funciton
 *
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
export const sum = (a: number, b: number): number => {
  // return a + b;
  return a + b + 9; // Add some number to fail test ;)
};
